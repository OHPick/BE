spring:
  profiles:
    include: secret
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
#        hbm2ddl:
#          auto: create